# 木子AI 网站项目

一个基于 **GitHub Pages** 构建的内容型网站，支持自动文章列表、分页、标签系统与配置文件化管理。

---

## 🚀 部署步骤

1. 上传整个项目到你的 GitHub 仓库根目录（仓库名为 `plus`）。
2. 打开仓库 → Settings → Pages → Source → 选择 `main` 分支 + `/root`。
3. 保存后访问：
